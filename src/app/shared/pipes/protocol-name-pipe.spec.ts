import { ProtocolNamePipe } from './protocol-name-pipe';

describe('ProtocolNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProtocolNamePipe();
    expect(pipe).toBeTruthy();
  });
});
