import { ThreatLevelPipe } from './threat-level-pipe';

describe('ThreatLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new ThreatLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
