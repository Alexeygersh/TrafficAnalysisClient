import { HighlightThreat } from './highlight-threat';

describe('HighlightThreat', () => {
  it('should create an instance', () => {
    const directive = new HighlightThreat();
    expect(directive).toBeTruthy();
  });
});
